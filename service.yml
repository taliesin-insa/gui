apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: gitlab-managed-apps
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 9501
